import {createSlice} from "@reduxjs/toolkit";

export const backlogSlice = createSlice({
    name: "comments",
    initialState: {
        backlogData: []
    },
    reducers: {
       
    },
    extraReducers: {
        


    }

})
